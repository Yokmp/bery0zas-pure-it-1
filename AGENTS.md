# bery0zas-pure-it-updated: Codex project context

Before starting work, read and apply ../AGENTS.md at the shared mods root. If this is a separate checkout/worktree or that file is absent, read F:\Games\Factorio_ModTest\mods\AGENTS.md instead. This explicit read is needed because Codex discovery may stop at this project's Git root. Then apply the project context below.

Read README.md for air-filtering behavior and roadmap context, then inspect current pollution/runtime code before changing it. Use this mod's tools/test/harness.json with the shared runner; the Pure It document in Toolsets/testharness/docs is an example, not a replacement for the actual configuration.

Keep the referenced documentation as the source of detailed context; do not duplicate it here. Inspect any additional relevant Markdown files before editing.
